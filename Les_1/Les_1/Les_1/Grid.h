#pragma once
#include <string>
#include <iostream>

class Grid
{
public:
	void DrawGrid(Docent docent, Student student);
	void AdjustGrid();

};

void Grid::DrawGrid(Docent docent, Student student)
{
	if (docent != null)
	{
		string[] modules = Docent.modules
	}
	if(student != null)
	{
		string[] modules = Student.modules
	}
	//Teken een grid dvm de modules die hier staan. Deze bevatten de tijden en kunnen zo in het grid worden geplaatst.

}