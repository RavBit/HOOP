#include "Grid.h"
#include "Module.h"
#include <vector>
#include <iostream>


using namespace std;


void Grid::DrawGrid(vector<Module> modulelist)
{
	//Teken een grid met de vector. Deze bevatten de tijden en kunnen zo in het grid worden geplaatst.

	//Voor het grid kan je het beste een X en een Y aannemen. Misschien een veld verdelen om het roostersysteem echt in te maken

}

void Grid::AdjustGrid(Module _mod)
{
	for each (Module mod in ML)
	{
		if (_mod.name == mod.name)
		{
			//REPLACE OR ADJUST THE MODULE
		}
	}
};
void Grid::RemoveGrid(Module _mod)
{
	for each (Module mod in ML)
	{
		if (_mod.name == mod.name)
		{
			//REMOVE THE MODULE
		}
	}
};